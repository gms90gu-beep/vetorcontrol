/**
 * syncEngine.ts — stub mínimo.
 * Substitua pelas implementações reais quando disponíveis.
 */

export async function processQueue(): Promise<void> {
  // TODO: drenar fila de mutações offline
}

export async function pullFromServer(_userId: string): Promise<void> {
  // TODO: puxar dados atualizados do servidor
}
